// all the things inside the document
console.dir(document);
//alert("hahahah");

// log domain, url, title
console.log(document.domain);
console.log(document.url);
console.log(document.title);

document.title = "Title changed"

console.log(document.all); // log all the elements
console.log(document.body); // log every element inside body

console.log(document.forms); // grab all forms
